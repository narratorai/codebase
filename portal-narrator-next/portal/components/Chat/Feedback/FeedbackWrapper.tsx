import { Space } from 'antd-next'
import styled from 'styled-components'

const FeedbackWrapper = styled(Space)`
  justify-content: end;
  position: absolute;
  height: 32px;
  padding: 4px;
  bottom: 0;
  right: 0;
`
export default FeedbackWrapper
