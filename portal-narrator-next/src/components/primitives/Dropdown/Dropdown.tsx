'use client'

import { Menu as HeadlessMenu, MenuProps as HeadlessMenuProps } from '@headlessui/react'

const Dropdown = (props: HeadlessMenuProps) => <HeadlessMenu {...props} />

export default Dropdown
